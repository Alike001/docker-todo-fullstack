services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - todo-data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  todo-data:
